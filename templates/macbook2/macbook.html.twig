{% extends 'base.html.twig' %}

{% block title %}PC macbook{% endblock %}
{% block monTitre %}Tous les portables macbook{% endblock %}
{% block body %}


 <div class="row">
        {% for pc_macbook in macbook %}
            <div class="col-12 col-md-6 col-lg-4 col-xl-3">
                <div class="card border-light mb-3">
                    <div class="card-header text-center">
                        <img src="{{asset('img/' ~ pc_macbook.image)}}" width="300" height="300">
                    </div>
                    <div class="card-body">
                        <h4 class="card-title">
                        <a href="{{ path('afficher_macbook', {'id': pc_macbook.id}) }}" >{{ pc_macbook.marque }} </a>
                        </h4>
                        <p>prix : {{ pc_macbook.prix }} €</p>
                        
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>

{% endblock %}
